mixin footer(data= {})
  -
    // Globals: COMPANY_NAME, DEFS, MENU

    const {
      menuActiveIndex,
      isSigned
    } = DEFS;

    const {
      className = ''
    } = data;

  footer.footer(class=className)
    +container()
      nav.menu
        ul.menu_nav
          each menuItem, index in MENU
            if menuItem.isInFooter
              - if (menuItem.isIfSignedIn && isSigned || !menuItem.isIfSignedIn)
                li(class= index === menuActiveIndex ? 'active' : '' class=menuItem.className)
                  a(href=menuItem.href )!= menuItem.title
      .bottom
        .copy!=`© 2018 ${COMPANY_NAME}`
        a.agreement(href="#") Политика конфиденциальности
      .create
        a(href="http://pink-man.ru" target="_blank") Сайт сделал –
          +svg({iconName: 'logo-pinkman'})


